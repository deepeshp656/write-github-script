
name: Label

on:
  pull_request:
env:
  check: 0  
jobs:
  varia:    
    
    runs-on: ubuntu-latest
    steps:
     - name: 'Comment PR'
       run: echo "hello variable $check"
       
  comment:
     
     runs-on: ubuntu-latest
     steps:
     - name: 'Comment PR'
       if: github.event_name == 'pull_request'
       run: echo "variable $check"
